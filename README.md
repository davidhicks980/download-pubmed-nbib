# Getting started

## Install dependencies  

`npm i`  
`npm i -g ts-node` 

## Sample run

`npm run start`

nbib files will be stored in src/output

## To build (for deployment or otherwise)

`npm run compile`

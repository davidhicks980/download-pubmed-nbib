# Getting started

Install dependencies
`npm i`
`npm i -g ts-node` <= to test without building

# To test

`npm run start`

nbib files will be stored in src/output

# To build (for deployment or otherwise)

`npm run compile`
